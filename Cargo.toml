[package]
name = "idle_dungeons"
version = "0.2.3"
edition = "2021"

[dependencies]
bevy = { version = "0.18", default-features = false, features = ["2d"] }
rand = "0.8"
rand_chacha = "0.3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"

[dev-dependencies]
tempfile = "3"
